cd
daffjl
print -p
print -h
print --help
print -Pn "some"
d
history 
history | tail -n1
echo $(history | tail -n1)
echo $(history)
echo $(history|tail)
echo $(history|tail -n1)
echo $(history|tail -n2)
history
git chekout
git stash
git revert HEAD
git stash --he
git stash apply
rm -rf .git
git init
touch some.py
git add some.py
git commit -m "first commit"
vim some.py
export kernel=$(uname -r)
echo '\
df
echo "$kernel"
echo "$kernel$kernel"
git commit -am "2nd commit"
source .zshrc
touch hello.py
source ~/.zshrc
git add hello.py
git reset HEAD hello.py
git rm hello.py
rm hello.py 
git rm some.py
git reset
git reset HEAD some.py
git reset HEAD
git checkout
git commit -m "blunder"
git checkout some.py
touch hellp.py
git add  *
git checkout -b feature
ls
git commit -m "feature branch"
git status
set | grep -i prompt
touch socket.py
git add .
command git status --porcelain -b
git commit
git commit -m "cleaning"
exit 
echo $vcs_info_msg_0_
vcs_info
$(echo "hello")
$(echo "date")
echo afdaf
echo $(date)
echo $(date) | grep  2020
echo $(date) | grep  '2020'
echo $(date) | grep  '2021$'
echo $(date) | grep  '2020$'
echo $(date) | grep  '^.+?:'
echo $(date) | grep  '20$'
echo $(date) | grep  ':'
echo $(date) | grep  '.+:'
echo $(date) | grep  '^.+:'
echo git:feate | grep  '^.+:' 
echo 'git:feate' | grep  '^.+:' 
echo 'git:feate' | grep  '^.+' 
echo 'git:feate' | grep  '^g' 
echo 'git:feate' | grep  '^.*' 
echo 'git:feate' | grep  '^.*?:' 
echo 'git:feate' | grep  '^git:' 
if echo $(date) | grep '20$'; then echo ok fi\
echo ok\
fi
if echo $(date) | grep '20$'; then; echo ok fi        
if $(echo $(date) | grep '20$'); then; echo ok ;fi        
if echo $(date) | grep '20$'; then; echo ok ;fi        
clear
if [ -d .git]; then; echo .git; fi 
if [ -d .git ]; then; echo .git; fi 
if [ -d .git]; then; echo .git; fi exit
cd tmp
if [ -d .git ]; then; echo .git; fi exit
if [ -d .git ]; then; echo .git; fi
celar
exit
cd tmp
touch hellp.py
vim hellp.py
git add hellp.py
git commit -m "adf"
git rm hellp.py
touch somefile.py
vim socket.py
ls
git status
git commit -a -m "cla"
git add .
touch yetanotherfile.py
clear
